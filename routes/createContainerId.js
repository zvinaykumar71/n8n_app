import express from 'express';
import { createContainer, publishContainer } from '../utils/GetContainerId.js';


const router = express.Router();

// router.route('/create-media').post(createContainer);
// router.route('/publish-media').post(publishContainer);



export default router;
